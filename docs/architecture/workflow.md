# Рабочий процесс задач

## Назначение

Описывает организацию процесса работы над задачами: участие ролей, правила и файловую реализацию в `tasks/`. Канонический жизненный цикл (состояния и переходы) — [state-machine.md](state-machine.md); этот документ его не дублирует.

## Содержание

### Канонический жизненный цикл

Задача проходит состояния: Backlog → Ready → In Progress → Review → Testing → Done → Archived, с особыми состояниями Blocked и Cancelled. Диаграмма, полная таблица переходов, guard-условия и инварианты — [state-machine.md](state-machine.md).

### Участие ролей по стадиям

| Стадия | Ответственная роль | Что делает |
| --- | --- | --- |
| Backlog | Project Manager | Фиксирует и готовит задачи, приоритизирует |
| Ready | Project Manager | Подтверждает Definition of Ready |
| In Progress | Developer | Реализует задачу, оформляет PR |
| Review | Reviewer | Проверяет PR ([review-process.md](../development/review-process.md)) |
| Testing | QA Engineer | Проверяет поведение и качество ([QA-чеклист](../../.claude/checklists/QA.md)) |
| Done | QA Engineer | Подтверждает Definition of Done |
| Blocked | Project Manager | Организует снятие блокировки |
| Cancelled / Archived | Project Manager | Отменяет с причиной, архивирует |

Обязанности ролей: [.claude/agents/](../../.claude/agents/).

### Контрольные точки человека

**Контрольная точка — состояние, из которого платформа не выходит сама.** Это не отдельное состояние жизненного цикла и не расширение [state machine](state-machine.md): состояния те же, но переход из них не выполняет никакая автоматика — его совершает человек.

Таких точек две, и они заданы [ADR-007](../adr/ADR-007-pm-qa-executors.md):

| Точка | Состояние | Решение человека | Переход | Команда |
| --- | --- | --- | --- | --- |
| Приём Definition of Ready | Backlog | Задача готова к работе | Backlog → Ready (`TaskPlanned`) | `POST /projects/{projectId}/tasks/{id}/plan` |
| Приёмочное решение | Testing | Definition of Done выполнен | Testing → Done (`TestsPassed`, затем `TaskCompleted`) | `POST /projects/{projectId}/tasks/{id}/complete-testing` |

Отрицательные исходы обеих точек уже выражены существующими переходами (`TaskReturnedToBacklog`, `TestsFailed`) — отдельных команд для них не требуется.

**Приёмочное решение имеет внешний эффект:** при положительном исходе платформа сливает Pull Request в основную ветку ([ADR-008](../adr/ADR-008-git-policies.md), порядок `TestsPassed` → `MergeCompleted` → `TaskCompleted`). Это необратимо в рамках задачи, поэтому интерфейс обязан обозначать последствие до подтверждения.

**Гарантия процессная, а не механическая.** Платформа **не отличает человека от агента** на этих командах: аутентификации нет ([ADR-012](../adr/ADR-012-identity-and-auth.md) — доверенная однопользовательская установка). Фактически агента удерживает **сетевой allowlist агентской песочницы** ([ADR-006](../adr/ADR-006-agent-execution-environment.md)): из неё достижимы только провайдер и GitHub, но не `apps/api`. Отсюда следствие, которое нельзя упустить: **открытие агентам доступа к `apps/api` снимает единственную реальную защиту этих точек** — какие команды при этом остаются агенту недоступны, требует отдельного решения.

Дополнительно, на уровне кода: адаптер `agents/claude-code` отказывается запускать роли Project Manager и QA Engineer (`ErrUnsupportedRole`) — ещё до старта песочницы. Автоматизация этих ролей идёт только после того, как решения станут видимыми и принимаемыми ([EPIC-012](../roadmap/EPIC-012-human-checkpoints.md)); роли Developer и Reviewer автоматизированы и контрольными точками не являются.

### Правила процесса

1. Задача — markdown-файл по шаблону [Task.md](../../.claude/templates/Task.md); история переходов, замечания и отчёты накапливаются в самом файле.
2. В работу берутся только задачи из `ready/` (DoR проверен, см. [CONSTITUTION.md](../../CONSTITUTION.md)).
3. Одна задача — один исполнитель роли Developer и один PR.
4. Возврат с ревью или тестирования: задача возвращается в In Progress с замечаниями/дефектами, зафиксированными в файле задачи.
5. Блокировка и отмена всегда сопровождаются записью причины; для блокировки — также требуемого решения.
6. Допустимость перехода определяется правилами state machine; нарушение — ошибка процесса.

### Файловая система `tasks/`

**Принято ([ADR-004](../adr/ADR-004-task-storage.md)):** целевая модель — PostgreSQL как источник истины, Task Engine как единственная точка переходов, `tasks/` — markdown-экспорт.

**Переходный период** (до ввода Task Engine): стадиям соответствуют каталоги `backlog/`, `ready/`, `in-progress/`, `review/`, `blocked/`, `done/`, `archive/`; перемещение файла = переход состояния; Testing — в `review/` (запись в «Истории»), Cancelled — в `archive/` (причина в «Истории»).

### Статус решений

Все решения, затрагивающие этот документ, приняты — полный индекс: [DECISIONS_INDEX.md](../adr/DECISIONS_INDEX.md) (18 из 18 принято на 2026-07-23).

- [ADR-004](../adr/ADR-004-task-storage.md) — **принято**: PostgreSQL — источник истины, `tasks/` — экспорт (см. выше).
- [ADR-011](../adr/ADR-011-task-identifiers.md) — **принято**: `TASK-NNN`/`EPIC-NNN`, последовательные в рамках проекта.
- [ADR-007](../adr/ADR-007-pm-qa-executors.md) — **принято**: обе роли (PM, QA) — агенты, человек подтверждает на контрольных точках (см. раздел «Контрольные точки человека» выше).

## Статус

Актуален

## Последнее обновление

2026-07-19
