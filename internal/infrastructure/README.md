# Слой: internal/infrastructure

## Назначение

Infrastructure Layer — адаптеры, реализующие контракты `internal/platform` и порты доменных модулей для конкретных технологий: PostgreSQL (персистентность, журнал событий), In-Memory Event Bus ([ADR-002](../../docs/adr/ADR-002-event-delivery.md)), GitHub (RepositoryProvider), файловая память / Qdrant (MemoryProvider). Заполняется эпиком Infrastructure (после Application Layer).

## Содержание

### Ответственность (целевая)

- Реализации контрактов `internal/platform` и портов доменных модулей.
- Никаких доменных решений: адаптер исполняет, домен решает.

### Зависимости

- Разрешено: контракты `internal/platform` и порты domain; драйверы своей системы; `pkg/`.
- Запрещено: доменная логика; зависимость адаптеров друг от друга; реализация «чужих» портов заодно.

### События

In-Memory Event Bus доставляет события; журнальный подписчик сохраняет их в PostgreSQL. Сами адаптеры доменных событий не порождают.

### Текущее состояние

Пусто (только этот README) — код появится в эпике Infrastructure.

## Статус

Актуален

## Последнее обновление

2026-07-19
