# Changelog

## Назначение

История заметных изменений проекта. Ведётся по формату [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/); версионирование — по [Semantic Versioning](https://semver.org/lang/ru/).

## Содержание

Типы изменений:

- **Added** — новая функциональность.
- **Changed** — изменения существующей функциональности.
- **Deprecated** — функциональность, которая будет удалена.
- **Removed** — удалённая функциональность.
- **Fixed** — исправления ошибок.
- **Security** — исправления уязвимостей.

### [Unreleased]

#### Added

- Структура репозитория (v0.1 Foundation).
- Базовая документация: README, CONSTITUTION, CLAUDE, ROADMAP, CHANGELOG.
- Архитектурная документация (`docs/architecture/`).
- Документация процессов разработки (`docs/development/`).
- Шаблон ADR (`docs/adr/ADR-000-template.md`).
- Система задач (`tasks/`) с описанием жизненного цикла.
- Роли, команды, шаблоны, чек-листы и контекст для AI-агентов (`.claude/`).
- Architecture v0.2: доменная модель, ядро, интерфейсы, границы модулей, state machine задачи, каталог событий, потоки данных, инженерные принципы (`docs/architecture/`) с Mermaid-диаграммами.
- Заготовки архитектурных решений ADR-001…ADR-014 со статусом Decision Required (`docs/adr/`).
- EPIC-002 Foundation: Go-модуль `ai-studio-os` (go.mod, `.golangci.yml`, Makefile) и контракты ядра — интерфейсы без реализации: `internal/core` (EventBus, Agent, Tool, Workflow, RepositoryProvider, MemoryProvider, словари Role/TaskState) и доменные пакеты `internal/domain/{task,project,event,workflow}` (TASK-001…TASK-011).
- По итогам code review EPIC-002: слоистая структура `internal/{core,domain,application,infrastructure}`; Agent → `Execute(ctx, Request) (Response, error)` с абстрактными Request/Response до ADR-005; Engine/Reader → Commands/Queries; `make verify` (gofumpt → golangci-lint → vet → test → markdownlint → docs/Mermaid, `scripts/verify-docs.sh`); README в каждом модуле; каталог `engineering/` (reviews, retrospective, decisions, metrics); процесс «план → утверждение → код → ревью → merge» закреплён в CLAUDE.md.
- ADR-015 (принят): `internal/core` упразднён — платформенные абстракции → `internal/platform` (EventBus, Agent, Tool, MemoryProvider, RepositoryProvider), язык домена → `internal/domain/shared` (Role, TaskState), контракт Workflow → `workflow.Rules`; слои domain → application → platform → infrastructure.
- Управленческое ревью EPIC-002.5 (2026-07-20): ROADMAP.md пересмотрен под релизные вехи по слоям архитектуры (v0.2 Architecture & Engineering Platform → v1.0 First Public MVP); для Domain Layer (v0.3) введено обязательное утверждение спецификации (назначение, инварианты, допустимые состояния, события, Acceptance Criteria) до реализации; EPIC-002.6 Developer Experience утверждён к исполнению.
- ADR-001 (принят): лицензия **Apache License 2.0**; полный текст в LICENSE.
- Двухуровневая документация модулей: краткий README в модуле + полная спецификация в `docs/specifications/{domain,application,platform,infrastructure}` (шаблон Specification.md); правило нового пакета: README + Specification + TASK + Acceptance Criteria.
- Уровни проверок: pre-commit (fmt+lint+vet) → pre-push (`make verify`) → GitHub Actions (`make verify`, без исключений); devcontainer подтверждён (минимальный состав); `.gitattributes` с нормализацией окончаний строк.
- Правило необратимости архитектурных решений закреплено в шаблоне ADR и engineering/decisions.
- Паспорт проекта PROJECT_MANIFEST.md, прогресс PROJECT_HEALTH.md, индекс решений docs/adr/DECISIONS_INDEX.md.
- Запрет прямых коммитов в `main` без исключений (feature branch → PR → Review → Merge, даже соло; ревью может выполнять Claude).
- Метрики: `make metrics` (scripts/metrics.sh) — снимки в engineering/metrics/; первый снимок 2026-07-20.
- EPIC-002.5 Engineering Platform: репозиторий опубликован на GitHub ([nikibrdev/ai-studio-os](https://github.com/nikibrdev/ai-studio-os), public, Apache-2.0); GitHub Actions `verify` — обязательный статус-чек на каждый PR и push в main (Go 1.24, gofumpt, golangci-lint, markdownlint, docs-check); CODEOWNERS; шаблоны Issue (bug/feature/task) и тип коммита `ci`; защита ветки `main` (обязательный чек, запрет прямого push/force-push/удаления, `enforce_admins`); детальный конфиг markdownlint (MD060 включён, таблицы нормализованы); проверка Conventional Commits в CI (`scripts/check-commits.sh`); Dependabot (gomod, github-actions); релизный процесс — категоризация release notes по типам коммитов (`.github/release.yml`), метки типов в репозитории, раздел «Релизный процесс» в git-workflow.md (TASK-012…020).
- ADR-001 pinned CI tool-version bug (BUGFIX-001/002): `gofumpt`/`golangci-lint` в CI незаметно собирались на Go 1.25 вместо заявленного 1.24 из-за `GOTOOLCHAIN=auto` — обнаружено на Dependabot-обновлении `actions/setup-go`; закреплены совместимые версии (gofumpt v0.9.2, golangci-lint v2.8.0) и `GOTOOLCHAIN: local` на уровне job, чтобы расхождение больше не маскировалось.
- EPIC-002.6 Developer Experience: git-хуки `pre-commit`/`pre-push` (`.githooks/`, `make install-hooks`) — реально протестированы негативными сценариями; `.editorconfig`; рекомендуемые настройки VS Code (`.vscode/extensions.json`, `settings.json`); минимальный Dev Container (`.devcontainer/`) — реально собран и проверен (`devcontainer up`/`exec`, `0 issues.`), базовый образ переведён на Docker Hub `golang:1.24-bookworm` после обнаруженной недоступности `mcr.microsoft.com` из сети разработки; `CONTRIBUTING.md` — проверен сквозным клоном в чистую директорию (TASK-021…025).

#### Changed

- Канонический жизненный цикл задачи расширен состояниями Testing и Cancelled (`docs/architecture/state-machine.md`); зависимые документы синхронизированы.
- Обновлены по итогам аудита: overview, system-design, components, event-model, workflow (`docs/architecture/`), глоссарий и контекст (`.claude/context/`), CLAUDE.md, README каталогов `tasks/`.
- **Architecture Freeze (2026-07-19)**: приняты ADR-002 (In-Memory Event Bus), ADR-003 (REST API), ADR-004 (PostgreSQL — источник истины задач, `tasks/` — экспорт), ADR-009 (Go 1.24, Next.js 15, pnpm, golangci-lint, gofumpt), ADR-014 (все проходят через Core); документация синхронизирована с решениями.
- **ADR-005 принят (Executor Contract, 2026-07-20)**: Agent (логическая роль) и Executor (реальный технический бэкенд) разделены как понятия; контракт `internal/platform` — ровно четыре возможности (`Accept`, `Artifacts`, `Status`, `Finish`); `internal/platform/agent.go` переименован в `executor.go` (`Agent` → `Executor`, `Request`/`Response` → `ExecutorTask`/`Artifact`/`ExecutionStatus`, по-прежнему абстрактны до Domain Layer); терминология синхронизирована во всей документации, ссылавшейся на старый путь ADR или `platform.Agent` (TASK-026).
- **Доменные основания довершены (2026-07-20, TASK-027)**: Execution признан не Bounded Context, а сквозной технической возможностью Application Layer — Bounded Contexts сведены к четырём (Planning, Development, Review, Knowledge — переименован из Memory); Artifact закреплён как первичная сущность результата работы (не Result — сущность Result убрана из доменной модели, Execution напрямую производит Artifact и несёт ExecutionStatus); домен-модуль `agent` переименован в `executor`; зафиксирован порядок проектирования Domain Layer — Artifact → Execution → Executor → Task → Project; VISION.md усилен дословной цитатой архитектора о платформе как «операционной системе исполнения инженерной работы».
- **ADR-016 принят (Artifact как Aggregate Root, 2026-07-20, TASK-028)**: Artifact закреплён как самостоятельный Aggregate Root — не часть Execution, не часть Task, не часть Project; модель `Project ├── Task └── Artifact`, `Execution` только ссылается на произведённый им Artifact, никогда им не владеет; введены Metadata (ID, Type, Author, CreatedAt, ProducedByExecution, Version) и Payload (тип-зависимое содержимое); зафиксированы границы (что НЕ является Artifact — временный лог, прогресс, heartbeat, токен LLM, внутреннее сообщение агента); концептуальный список доменных модулей вырос до 11 (добавлен `artifact`); открытый вопрос ADR-005 о размещении Artifact закрыт.
- **EPIC-003 Domain Layer открыт (2026-07-20)** в режиме «Domain Specifications First» ([docs/roadmap/EPIC-003-domain-layer.md](docs/roadmap/EPIC-003-domain-layer.md)): этап 1 — без единой строки Go, полные спецификации пяти модулей в порядке Artifact → Execution → Executor → Task → Project (TASK-029…033, `tasks/ready/`); реализация (этап 2) не начинается без утверждения архитектором всех пяти.
- **Спецификация Artifact принята со статусом Reference (2026-07-20, TASK-029, PR #34/#35/#36)** — первая доменная спецификация проекта и первая, официально признанная образцом для всех последующих (`docs/specifications/domain/artifact.md`): One Sentence/Identity определяют сущность до текста о назначении (Model First); Invariants разделены на Structural/Behavioral; Lifecycle — Draft → Published → Archived; Origin и Author разведены как разные понятия; Domain Events/Commands/Queries/Examples выведены как следствие уже принятой модели; Acceptance Criteria проверяют качество модели, а не факт заполнения разделов; Future Extensions разделены на Ожидаемые/Возможные; добавлен раздел Stability Assessment (20-й, финальный). Введены статус **Reference**, обязательный **Delta Review** для TASK-030 и далее, и защита утверждённых спецификаций (изменение — только через новый ADR, отдельный Domain Revision PR или обоснованное изменение с Delta Review). Зафиксирован внутренний архитектурный рубеж «First Reference Domain Specification» (ROADMAP.md) — не пользовательская версия.
- **Этап 1 EPIC-003 (Domain Specifications First) закрыт (2026-07-21, TASK-030…033)** — спецификации Execution, Executor, Task и Project утверждены (`docs/specifications/domain/{execution,executor,task,project}.md`), каждая тремя PR с обязательным Delta Review против уже утверждённых. Final Architecture Review внёс по одному содержательному изменению в каждую: Execution — гонка команд Fail/Aborted разрешена порядком выполнения (Behavioral Invariant); Executor — добавлен прямой переход Registered → Retired; Task — расхождения с уже принятым `contracts.go` (`epicID`, запись scope/критериев приёмки) признаны целевой моделью, контракт целенаправленно расширяется на этапе 2, а не наоборот; Project — гипотеза о неявном триггере Created → Active заменена явной командой Activate с проверяемым guard-условием, отсутствие отключения Repository формализовано как сознательное ограничение v1. Все пять спецификаций Domain Layer теперь утверждены; этап 2 (реализация на Go) открывается отдельными задачами.
- **EPIC-003 Domain Layer закрыт целиком — v0.3 завершён (2026-07-21, TASK-034…039, PR #42–#47)** — реализованы все пять доменных сущностей строго по утверждённым спецификациям (единый стиль: сущность + методы-команды, возвращающие события значениями; sentinel-ошибки; ссылки между модулями — идентификаторы-строки по ADR-015; без Commands/Queries-интерфейсов до появления потребителя): `internal/domain/artifact` (Draft → Published → Archived, immutability после Publish), `execution` (гонка Fail/Abort — порядком команд), `executor` (реестр бэкендов, набор Role никогда не пуст), `task` (сущность + расширение контракта `epicID`/SetScope/SetAcceptanceCriteria; правки только в Backlog; допустимость переходов решает исключительно `workflow.Rules`), `project` (явная команда Activate с guard «≥1 Repository»). Каноническая state machine реализована (`workflow.Machine`, ровно 20 переходов, исчерпывающий перебор 81 пары, 100% покрытия) по решению [2026-07-21-workflow-rules-canonical-source.md](engineering/decisions/2026-07-21-workflow-rules-canonical-source.md) — без дублирующей спецификации, state-machine.md признан каноническим источником уровня спецификации. Итог подтверждён сквозным сценарием слоя (`internal/domain/goldenpath_test.go`): Task проходит все девять канонических состояний через реальную Machine, порождая Execution и опубликованный Artifact; всего 82 unit-теста Domain Layer.
- **Три архитектурных блокера сняты — ADR-011, ADR-008, ADR-006 приняты (2026-07-21, PR #49/#50/#51)**: идентификаторы `TASK-NNN`/`EPIC-NNN` последовательные в рамках Project с суррогатным ключом в БД (существующая практика легализована без миграции); git-политики — merge commit, слияние **после** Testing (порядок TestsPassed → MergeCompleted → TaskCompleted), 1 обязательный ревьюер (агент допустим); среда агентов — Docker-контейнер на Execution (сеть по allowlist, короткоживущие секреты), до v0.6 — только локально под надзором человека. Принято 12 ADR, открыто 4 (007/010/012/013), ни один не блокирует v0.4–v0.6.
- **EPIC-004 Application Layer закрыт целиком — v0.4 завершён (2026-07-21, TASK-040…045, PR #53–#57)** — реализованы четыре use-case-сервиса золотого пути поверх Domain Layer: `TaskPlanningService` (постановка задачи), `WorkService` (запуск работы — порождение Execution), `ResultService` (производство результата — Artifact + завершение Execution), `CompletionService` (Review → Testing → Done). ADR-008 выражен кодом, не только текстом: `CompleteTesting` публикует `TestsPassed` → вызывает `RepositoryProvider.MergePullRequest` → публикует `MergeCompleted` → и только тогда переводит задачу в Done с `TaskCompleted`; отказ merge — код-гейт, задача остаётся в Testing. `TaskProjection` строит читаемое состояние задачи только из событий (ADR-014) и доказанно пересобираема с нуля. Попутно закрыты два пробела: каталог `internal/domain/event` не включал 16 событий Artifact/Execution/Executor/Project из утверждённых спецификаций EPIC-003 (добавлены, `events.md` синхронизирован); `platform.Event` не оставлял места для данных, специфичных для события (`Envelope.WithData`, метод сверх контракта, не изменяющий его). Сквозной тест приложения (`internal/application/e2e_test.go`) проводит задачу через весь golden path на in-memory адаптерах, включая ветки «changes requested» и «tests failed». Обнаружено и задокументировано (не устранено — решение для EPIC-005) отсутствие межагрегатной транзакции при последовательном сохранении нескольких агрегатов в одном use-case. 136 unit-тестов во всём проекте.
- **EPIC-005 Infrastructure Layer закрыт целиком — v0.5 завершён (2026-07-21, ADR-017, TASK-046…051, PR #59–#65)** — те же use-case'ы EPIC-004, без единой строки изменений в `internal/application`/`internal/domain`, впервые работают на реальной инфраструктуре вместо in-memory фейков. ADR-017 (принят): драйвер PostgreSQL — `pgx/v5`, миграции — самописный раннер по встроенным `.sql` (без внешней библиотеки, тот же принцип, что и отказ от UUID-зависимости в EPIC-004). Реализовано: пять Postgres-адаптеров хранения (`ProjectStore`…`ArtifactStore`, upsert-семантика, `application.ErrNotFound`); производственный `EventBus` (та же семантика доставки, что и тестовый фейк EPIC-004) с журналом в PostgreSQL — `Publish` журналирует прежде доставки, `eventbus.ReadJournal` восстанавливает события из журнала для перестроения проекций (назначение журнала из ADR-002/event-model.md доведено до работающего кода); `RepositoryProvider` — адаптер GitHub REST API напрямую на `net/http` (без клиентской библиотеки), с двумя явно задокументированными решениями по неоднозначности контракта (целевая ветка PR всегда `main`; `RequestReview` — комментарий к PR, а не назначение GitHub-ревьюера, поскольку ADR-008 возлагает обязательность ревью на workflow, а не на GitHub); composition root (`internal/infrastructure/wiring`) собирает все адаптеры в одну систему. Golden path подтверждён интеграционным тестом на настоящем PostgreSQL (`TestGoldenPath_Infrastructure`) — три прогона подряд зелёные; новый CI-job `integration` с сервис-контейнером PostgreSQL, не блокирующий обязательный статус-чек `verify`. Попутно обнаружены и исправлены два пробела: раннер миграций был уязвим к гонке при конкурентном вызове `Migrate` (исправлено advisory lock'ом на выделенном соединении); пять доменных агрегатов не давали способа реконструкции из персистентных данных без бизнес-команд (добавлен `Restore(...)` в `project`/`task`/`executor`/`execution`/`artifact`, чистая функция без бизнес-правил и событий). Принятый открытый риск: `RepositoryProvider` не проверен вживую на реальном GitHub (нет тестового репозитория и токена в этой сессии) — покрыт unit-тестами на HTTP-уровне (89.6%), golden-path тест использует in-memory фейк для этой части с явной пометкой. 170 unit-тестов во всём проекте.
- **EPIC-006 AI Agent Runtime закрыт целиком — v0.6 завершён (2026-07-22, TASK-052…057, PR #67–#72)** — целевая модель исполнения из ADR-006 реализована и подтверждена реальным прогоном: платформа впервые запускает исполнителя сама, без человека за терминалом на шаге выполнения. `platform.ExecutorTask`/`Artifact`/`ExecutionStatus` конкретизированы (плоские структуры вместо `any`, `internal/platform` остался домен-агностичным — ADR-015). Docker-образ исполнения (`docker/execution`) — на базе Dev Container (`golang:1.24-bookworm`), с Claude Code CLI (Node 22.x из NodeSource — Debian-пакет не удовлетворяет требованию CLI `>=22`, обнаружено сборкой). `agents/claude-code/container.Manager` управляет контейнером Execution через `docker` CLI напрямую (`agents/` не может импортировать `internal/infrastructure`): сетевой allowlist — две Docker-сети и Squid-прокси между ними (контейнер исполнения физически не имеет иного пути наружу), секреты — только переменные окружения, токен читается `git`'ом через `GIT_ASKPASS`, не встраивается в URL/argv. `agents/claude-code.Executor` реализует `platform.Executor` (Accept/Artifacts/Status/Finish) поверх этого жизненного цикла. Демонстрационный реальный прогон (TASK-056) вскрыл и позволил исправить два реальных дефекта: контейнер не оставлял окна для `docker exec` между завершением команды и собственным выходом (исправлено — код возврата в файл, контейнер простаивает после команды вместо немедленного завершения); `docker exec` не указывал рабочую директорию, попадая не в клон, а в `WORKDIR` образа. После исправлений — стабильный честный прогон дважды подряд: реальный контейнер, реальный клон `nikibrdev/ai-studio-os`, реальная блокировка неразрешённого хоста, реальный сбор 20 артефактов-коммитов, чистое освобождение ресурсов. Принятый открытый предел: сам вызов AI-провайдера не проверен — в этой сессии нет отдельного `ANTHROPIC_API_KEY`, и извлечение/переиспользование учётных данных сессии для этой цели не рассматривалось (пользователь явно выбрал зафиксировать честный предел вместо предоставления ключа); адаптер уже готов принять реальный ключ без доработок кода. Auto-подбор исполнителя (Orchestrator) и Tool Layer — сознательно вне scope (ADR-007 Decision Required только для ролей PM/QA, не Developer; Tool Layer — v0.8). 202 unit-теста во всём проекте.
- **EPIC-007 Memory System закрыт целиком — v0.7 завершён (2026-07-22, ADR-018, TASK-058…063, PR #75–#78)** — уже принятый контракт `platform.MemoryProvider` реализован целиком, без изменения самого контракта: файлы (`memory/<projectID>/<id>.md`, frontmatter + Markdown, [decision-документ](engineering/decisions/2026-07-22-memory-file-format.md)) — durable источник истины; Qdrant — производный, перестраиваемый индекс (тот же принцип, что журнал событий и проекции чтения, EPIC-004/005). ADR-018 (принят): эмбеддинг — наивный детерминированный feature hashing (256 измерений, `hash/fnv`+`math`, без внешних зависимостей и секретов — у сессии нет ключа ни к одному провайдеру эмбеддингов, то же решение, что отказ от реального AI-вызова в EPIC-006); коллекция Qdrant `memory_entries` общая на все проекты, `project_id` — поле payload, а не отдельная коллекция (по аналогии с PostgreSQL-таблицами EPIC-005); payload самодостаточен для реконструкции записи без обращения к файлам. `internal/infrastructure/memory.Provider.Record` пишет файл, затем индексирует в Qdrant — в этом порядке, чтобы сбой Qdrant не терял данные; `Reindex` восстанавливает индекс проекта из файлов с нуля. Собран в `wiring.System.Memory` — best-effort по аналогии с `Repository` при отсутствии `GITHUB_TOKEN` (пустой адрес Qdrant оставляет `Memory` равным `nil`, а не ошибкой). Реально проверено вживую на настоящем Qdrant (не только на фейках): запись, поиск, изоляция между проектами (поиск в одном проекте не возвращает записи другого) и восстановление после симулированной потери индекса; CI-job `integration` дополнен сервис-контейнером Qdrant. Принятое честное ограничение MVP: поиск не семантический по сути — совпадение хешированных токенов, а не смысла; замена на реальную модель эмбеддингов при появлении провайдера/ключа — локализованное изменение одной функции, предмет отдельного будущего ADR. Политика устаревания и очистки памяти сознательно не решена — `Record` только добавляет записи, без опыта реальной эксплуатации любое правило было бы придумано, а не выведено из потребности. 253 unit-теста во всём проекте.
- **EPIC-008 API Layer закрыт целиком — v0.9 завершён (2026-07-22, ADR-012, TASK-064…071, BUGFIX-003, PR #80–#87)** — `apps/api` реализует весь golden path через REST (ADR-003) поверх уже готового `internal/application` (EPIC-004): 15 операций по четырём ресурсам (Projects/Tasks/Artifacts/Executions), написанных Documentation First (`docs/api/`), до реализации хендлеров. Порядок реализации — API раньше Dashboard, вопреки номеру версии: `apps/dashboard` структурно зависит от `apps/api` (module-boundaries.md), [decision-документ](engineering/decisions/2026-07-22-api-before-dashboard-build-order.md). ADR-012 принят (Вариант 1): аутентификация отложена — доверенная однопользовательская установка, пересмотр при появлении внешнего потребителя. Два точечных, явно обоснованных дополнения к уже закрытым эпикам: `ProjectService` (Application Layer, EPIC-004) — без него API не мог бы создавать проекты; генерация последовательного `TASK-NNN` на проект (Infrastructure Layer, EPIC-005) — ADR-011 требовал этого с v0.5, но реализация выдачи номера отсутствовала. **BUGFIX-003** (обнаружен живой HTTP-проверкой TASK-069, исправлен до закрытия эпика): публичный `TASK-NNN` уникален только в рамках Project (ADR-011), но `tasks.id` был единственным глобальным `PRIMARY KEY` — два разных проекта с одинаковым `TASK-001` молча портили данные друг друга через `ON CONFLICT (id) DO UPDATE`; исправлено составным ключом `(project_id, id)` по всему стеку (PostgreSQL → шесть методов Application Layer → `TaskProjection` → маршруты `apps/api`, вложенные под `/projects/{projectId}/tasks/...`), воспроизведено и подтверждено устранённым на реальном PostgreSQL. Реально проверено вживую: сквозной HTTP-тест (`TestGoldenPath_HTTP`, три прогона подряд) проводит задачу через весь путь на настоящем PostgreSQL, включая обе ветки ревью (одобрено/отклонено) и тестирования (успех/провал); `RepositoryProvider` — тот же фейк EPIC-004, что уже использовался в `TestGoldenPath_Infrastructure` (EPIC-005) — токена GitHub нет во всех окружениях, где это выполняется. Честные ограничения: без списковых операций сверх `TaskProjection.Get` по одной записи — решение по реальной потребности будущего Dashboard, не здесь. 285 unit-тестов во всём проекте.
- **EPIC-009 Dashboard закрыт целиком — v0.8 завершён (2026-07-23, ADR-009 дополнен, TASK-072…078, PR #90–#95)** — `apps/dashboard` (Next.js 15, TypeScript, App Router) показывает список проектов → задачи проекта → детали задачи (заголовок, тип, scope, критерии приёмки, состояние, время обновления) через реальные HTTP-запросы к `apps/api` — первая версия сознательно read-only (формы создания/действия отложены до реальной потребности), без аутентификации (ADR-012) и без канала реального времени (ADR-003 предвидел этот вопрос). Порядок реализации — вопреки номеру версии: строился после v0.9 API, поскольку `apps/dashboard` структурно зависит от `apps/api`. Frontend-толчейн зафиксирован дополнением [ADR-009](docs/adr/ADR-009-toolchain.md): Node.js 22 LTS, TypeScript, ESLint+Prettier, Vitest+React Testing Library, pnpm (версия закреплена через `packageManager` в `package.json`, тот же принцип, что и у Go-инструментов). По ходу эпика закрыты два реальных пробела API, обнаруженных при проектировании, а не изобретённых заранее: `apps/api` не умел выдавать списки — добавлены `GET /projects`/`GET /projects/{id}/tasks` (`ProjectStore.List`, `TaskProjection.ListByProject`, TASK-072); `TaskProjection` несла только состояние задачи, не её описание — расширена title/type/scope/acceptanceCriteria через `Envelope.WithData` на `TaskCreated` (тот же механизм, что уже применялся для `ReviewCompleted`, TASK-076) — `apps/api` по-прежнему не читает `TaskStore` напрямую (ADR-014). CI: `apps/dashboard` (`pnpm lint`/`format:check`/`test`/`build`) проверяется в том же обязательном статус-чеке `verify`, что и Go-код (TASK-077) — по ходу найдены и исправлены два реальных дефекта конфигурации (`pnpm/action-setup` требует явную версию pnpm; ищет `package.json` в корне репозитория, которого нет в Go-монорепозитории), и негативный сценарий (намеренно испорченный тест) подтверждён реальным красным прогоном перед закрытием задачи. Реально проверено вживую (Playwright, headless Chromium, на настоящем `apps/api`+PostgreSQL): полный путь навигации, кириллица отображается верно, недоступный API и несуществующие проект/задача дают читаемую ошибку вместо падения страницы. 298 unit-тестов Go + 8 unit-тестов `apps/dashboard` во всём проекте.

- **EPIC-012 Контрольные точки человека закрыт — третий эпик декомпозиции v1.0 (2026-07-26, TASK-091…095 + BUGFIX-009/010, PR #116–#122)** — контрольные точки человека стали **определёнными, видимыми и действенными**. Разбор перед открытием показал, что команды подтверждения **уже существовали** (`plan`, `complete-testing`), а не хватало трёх других вещей: нормативного определения (в [`workflow.md`](docs/architecture/workflow.md) о контрольных точках не было **ни слова**, хотя ADR-007 назвала его затронутым — долг с момента её принятия), способа увидеть, что ждёт решения, и возможности действовать из UI. Добавлены: нормативный раздел `workflow.md` с таблицей «состояние → решение → переход → команда»; read-модель `ListAwaitingDecision` и операция `GET /decisions` ([docs/api/decisions.md](docs/api/decisions.md), спецификация до реализации); раздел «Ждут решения» и **первый мутирующий UI в проекте** — подтверждение обоих решений через Server Actions (`fetch` из браузера невозможен: у `apps/api` нет CORS-заголовков). **Каноническая state machine не менялась:** контрольная точка — это состояние, из которого платформа не выходит сама, и read-модели над существующим состоянием оказалось достаточно. Правило «какое состояние — контрольная точка» задано **явным перечислением двух**, а не выведено из `workflow.Rules.NextRole`: вывод из «того, что пока не автоматизировано» молча опустошил бы список при автоматизации PM/QA. Подтверждено **в реальном браузере** против настоящих `apps/api` + PostgreSQL: приём DoR перевёл задачу в Ready; приёмочное решение **слило настоящий Pull Request** в репозитории-песочнице и довело задачу до Done (порядок событий по ADR-008); возврат на доработку вернул в In Progress; отказной путь проверен **реальным** отказом state machine (`ready -> ready`) — интерфейс показал причину и не выдал устаревшее состояние за актуальное. **Цена честной проверки — два дефекта:** [BUGFIX-009](tasks/done/BUGFIX-009-pull-request-reference-lost.md) — платформа теряла ссылку на созданный Pull Request (`events.md` требовал её в `ReviewRequested`), из-за чего положительное приёмочное решение не мог принять **никто**; найден разбором перед написанием кода UI. [BUGFIX-010](tasks/done/BUGFIX-010-projection-not-rebuilt-on-startup.md) — read-модель не восстанавливалась при перезапуске процесса, и после рестарта API список ожидающих решения был пуст при непустой базе; средства (`ReadJournal`/`Rebuild`) существовали с EPIC-005 и не вызывались. **Названное ограничение:** платформа не отличает человека от агента на этих командах (аутентификации нет — ADR-012), фактическая защита — сетевой allowlist песочницы; открытие PM/QA-агентам доступа к `apps/api` её снимет, и это стало документированной предпосылкой четвёртого эпика. Добавлен шаг `pnpm typecheck` в CI: ни `pnpm test`, ни `pnpm build` не проверяют типы в тестах.
- **EPIC-011 Роли исполнителей закрыт — второй эпик декомпозиции v1.0 (2026-07-26, TASK-086…090 + BUGFIX-008, PR #109–#115)** — один адаптер `agents/claude-code` и **один** Docker-образ исполняют разные роли, различая их блоком инструкций промпта из `ExecutorTask.Role` ([ADR-007](docs/adr/ADR-007-pm-qa-executors.md) фиксирует именно это, не отдельные адаптеры). Автоматизирована роль **Reviewer**: на `ReviewRequested` Orchestrator запускает агента, тот изучает `origin/main...HEAD` и записывает вердикт в файл, платформа применяет его через `CompleteReview` — задача уходит в Testing либо обратно в In Progress. **PM и QA сознательно не автоматизированы:** ADR-007 называет контрольными точками человека ровно то, что производят они (приём DoR, финальное Done), а вердикт QA немедленно сливает Pull Request — поэтому их диспетчеризация перенесена в эпик после механизма подтверждения; иначе платформа доводила бы изменения до слияния сама, а документация утверждала бы, что человек подтверждает. Автоматический вердикт ревью к слиянию не ведёт: оно происходит на шаге QA. **Вердикт по умолчанию не выдумывается** — отказ агента, таймаут, отсутствующий или нераспознанный вердикт оставляют задачу в Review (выдуманное одобрение продвинуло бы код к слиянию без реальной проверки). Вердикт — не артефакт и контракт Executor не расширяет (ADR-005: четыре возможности; артефакт — свидетельство работы, вердикт — решение о работе). Закрыт Open Question EPIC-010: идентификатор записи исполнителя выводится из роли, и фабрика бэкенда получает ту же роль — запись реестра и запущенный бэкенд согласованы **по построению**, посторонние записи выбор не меняют (подтверждено вживую записями, сортирующимися раньше собственных). Попутно найден [BUGFIX-008](tasks/done/BUGFIX-008-qdrant-upsert-not-waited.md): интеграционный тест Memory проходил из-за собственного ретрая, маскируя отсутствие `wait=true` при записи в Qdrant. **Не проверено вживую:** применение вердикта — без `ANTHROPIC_API_KEY` агент не доходит до решения (покрыто юнит-тестами, критерий эпика отмечен частично). 409 функций-тестов Go плюс 11 подтестов.
- **EPIC-010 Orchestrator закрыт — первый эпик декомпозиции v1.0 (2026-07-25, TASK-079…085, PR #97–#107)** — `apps/orchestrator` реагирует на события жизненного цикла задачи и сам запускает исполнителя: платформа впервые доводит запланированную задачу до работающего агента без участия человека (`TaskPlanned` → выбор Active Developer-исполнителя → ветка `feature/TASK-NNN` в GitHub → `StartTask` → изолированная песочница с клоном → слежение до терминального состояния → уничтожение песочницы). Доменных правил и durable-состояния не содержит: каждый переход идёт через `internal/application`, допустимость решают модули `task`/`workflow`. **Подписка на шину невозможна** — продуктивная `eventbus.Bus` доставляет только внутри своего процесса (ADR-002), поэтому Orchestrator опрашивает журнал событий по курсору через новый порт `application.EventJournal` (временный шаг, явно привязанный к будущей настоящей шине в самой ADR-002). Курсор — по монотонной `seq` (миграция 0007), а не по времени: курсор по `occurred_at` **бесшумно терял бы события**, поскольку штамп ставится доменом до записи строки (обнаружено при разборе схемы до реализации, подтверждено регрессионным тестом). Свою `TaskProjection` Orchestrator наполняет из того же журнала (ADR-014: проекция — единственный путь чтения Task). Также добавлены `ExecutorService` (Register/Activate) и `ExecutorStore.List`. Новое: [docs/architecture/orchestrator.md](docs/architecture/orchestrator.md), [apps/orchestrator/README.md](apps/orchestrator/README.md). Подтверждено тремя живыми прогонами на настоящей инфраструктуре (PostgreSQL, Docker, приватный репозиторий, действительный токен, два отдельных процесса). **Осознанные ограничения:** только роль Developer; один исполнитель; курсор в памяти; слежение синхронное (доменная модель не отслеживает занятость исполнителя, поэтому синхронность выстраивает работу в очередь); участок «коммиты агента → Pull Request → Review» **вживую не пройден** — без `ANTHROPIC_API_KEY` агент не производит коммитов (покрыт юнит-тестами, критерий эпика отмечен частично). 420 unit-тестов Go во всём проекте.

#### Fixed

- **BUGFIX-004 — пересборка проекции из журнала теряла данные `Envelope.WithData`** (2026-07-25, PR #101): `applyCreatedData`/`targetState` приводили событие к конкретному типу `Envelope`, а события из журнала имеют тип пакета `eventbus` — при replay терялись все описательные поля задачи, а `ReviewCompleted` не двигал состояние. Исправлено структурным приведением (`dataCarrier`) — тем же приёмом, который `eventbus` уже применял на своей стороне границы. Дефект жил с EPIC-004: единственный тест, пересобиравший проекцию из реального журнала, проверял только финальное состояние `Done`, которое достигается по `TaskCompleted` и в прикреплённых данных не нуждается. Пробел закрыт.
- **BUGFIX-005 — GitHub-адаптер строил неверный путь API** (2026-07-25, PR #103): платформа хранит репозиторий как `github.com/owner/name` (этот формат используется в интеграционных тестах, `docs/api`, `ADR-013`), а адаптер подставлял значение в `repos/%s/...` как есть, получая `repos/github.com/owner/name/...` — **все шесть** операций GitHub были неработоспособны на реальном значении. Обнаружено первым живым прогоном Orchestrator'а (404 от GitHub). Заодно зафиксирован сам формат идентификатора, который ADR-013 должна была определить и не определила.
- **BUGFIX-006 — клон в песочнице строил URL с удвоенным хостом** (2026-07-25, PR #106): `https://github.com/github.com/owner/name.git` — **ни одно** реальное исполнение не могло начаться, все падали с `exit 128`. Тот же класс дефекта, что BUGFIX-005, во втором месте потребления того же значения; существовал с EPIC-006 и был невидим, потому что тесты песочницы подавали короткую форму `owner/name`, а платформа передаёт полную. Добавлена явная проверка формата с понятной ошибкой до старта контейнера.
- **BUGFIX-007 — `Artifacts` возвращал унаследованную историю как произведённые коммиты** (2026-07-25, PR #107): `git log` без диапазона отдавал историю ветки, унаследованную от базовой, — на любом репозитории с коммитами чужая работа записывалась как произведённая исполнением (`Origin: produced`) и по ней открывался Pull Request. Исправлено фиксацией `HEAD` при клонировании и запросом `git log <base>..HEAD`. Дефект жил с EPIC-006: фейковая песочница в тестах игнорировала аргументы команды, поэтому тесты проверяли разбор вывода, но не то, какую команду адаптер выполняет; в README прогон TASK-056 фиксировал чтение чужой истории **как успех**.

## Статус

Актуален

## Последнее обновление

2026-07-25
