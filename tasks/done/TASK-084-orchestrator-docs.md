# TASK-084: Документация apps/orchestrator

## Тип

docs

## Эпик

[EPIC-010 Orchestrator](../../docs/roadmap/EPIC-010-orchestrator.md)

## Цель

Задокументировать построенный механизм: README модуля, новый архитектурный документ о механизме диспетчеризации и курсорном опросе журнала, синхронизация `module-boundaries.md`/`components.md` с фактической реализацией.

## Контекст

По аналогии с README `agents/claude-code`/`apps/api`/`apps/dashboard` — описание должно позволить читателю понять механизм без чтения кода. `module-boundaries.md` уже содержит раздел `apps/orchestrator`, но написан до реализации (терминология «Core», без упоминания `EventJournal`) — тот же вид уточнения, что уже сделан для `apps/api` в EPIC-008/009.

## Scope

### Входит

- `apps/orchestrator/README.md` — назначение, переменные окружения, механизм (бутстрап исполнителя, курсорный опрос, диспетчеризация Developer), ограничения (курсор в памяти, один исполнитель, без ретраев), запуск локально.
- `docs/architecture/orchestrator.md` (новый, по единой структуре: Заголовок/Назначение/Содержание/Статус/Последнее обновление) — механизм диспетчеризации, курсорный опрос журнала вместо `Subscribe` (со ссылкой на обоснование в EPIC-010), диаграмма последовательности Developer-шага.
- `docs/architecture/module-boundaries.md` — раздел `apps/orchestrator`: уточнение терминологии «Core»→`internal/application` (как для `apps/api`), упоминание порта `EventJournal`.
- `docs/architecture/components.md` — при необходимости уточнить описание Orchestrator'а под фактическую реализацию.
- Ссылка на `docs/architecture/orchestrator.md` из `docs/architecture/README.md` (индекс), если там есть перечень документов раздела.

### Не входит

- Изменение кода — эта задача только документация.

## Критерии приёмки

- [x] `apps/orchestrator/README.md` написан по стандарту README модуля (по ходу TASK-081…083; здесь — замена заглушки на реальную ссылку).
- [x] `docs/architecture/orchestrator.md` создан, отражает фактическую реализацию (не аспирационное описание): включая исправленный порядок шагов и все ограничения с указанием, что нужно для снятия каждого.
- [x] `module-boundaries.md` (TASK-082)/`components.md` синхронизированы; дополнительно синхронизирован `golden-path.md`, который называл сценарий полностью аспирационным.
- [x] `make verify` (включая проверку ссылок и markdownlint) — чисто.

## Затрагиваемые модули и документы

- `apps/orchestrator/README.md`, `docs/architecture/orchestrator.md`, `docs/architecture/module-boundaries.md`, `docs/architecture/components.md`, `docs/architecture/README.md`.

## Definition of Ready

- [x] Цель и результат сформулированы
- [x] Критерии приёмки определены
- [x] Затрагиваемые модули/документы указаны
- [x] Ограничения и зависимости указаны — зависит от TASK-081…083 (описывает фактически построенное)

## План реализации

**Обнаружено при разборе:** большая часть исходного scope этой задачи уже выполнена по ходу TASK-081…083 — документация писалась в том же PR, что и код, а не откладывалась (правило CLAUDE.md: документация обновляется в том же PR, что и изменение). Уже готово:

- `apps/orchestrator/README.md` — назначение, структура, границы, переменные окружения, диспетчеризация, слежение, матрица отказов, все ограничения (TASK-081/082/083).
- `docs/architecture/module-boundaries.md` — раздел `apps/orchestrator` переписан: `internal/application` вместо устаревшего «Core», порт `EventJournal`, узкое исключение для composition root, строка сводной матрицы (TASK-082).

Остаётся:

1. `docs/architecture/orchestrator.md` (новый) — по единой структуре документов раздела (Заголовок/Назначение/Содержание/Статус/Последнее обновление, образец — `memory.md`/`tools.md`): роль в системе, обоснование опроса журнала вместо подписки, наполнение read-модели, sequence diagram участка `TaskPlanned` → Review, обоснование порядка шагов, принципы поведения при отказе, таблица ограничений с указанием, что нужно для снятия каждого.
2. `docs/architecture/components.md` — раздел Orchestrator: добавить ссылку на новый документ (в этом файле так же сделано для `memory.md`/`tools.md`) и привести описание к фактической реализации (сейчас — «назначение исполнителей ролей», хотя реализована одна роль Developer).
3. `apps/orchestrator/README.md` — заменить строку-заглушку «полное описание появится в TASK-084» на реальную ссылку.
4. `docs/architecture/golden-path.md` — участок Developer теперь автоматизирован; отметить это, поскольку документ описывал сценарий как полностью аспирационный.
5. `docs/roadmap/EPIC-010-orchestrator.md` — отметить выполненные критерии завершения.
6. `make verify` (в том числе проверка ссылок и mermaid).

**Не входит:** `docs/architecture/README.md` — такого файла в проекте нет (документы раздела связаны перекрёстными ссылками из `components.md`), поэтому пункт исходного scope про индекс неприменим; отмечено, чтобы отсутствие не выглядело пропуском.

## История

2026-07-23 — Architect — EPIC-010 открыт; задача поставлена в очередь, зависит от TASK-081…083.

2026-07-25 — Developer — при разборе выяснилось, что большая часть scope уже закрыта по ходу TASK-081…083; план сокращён до фактически оставшегося.

2026-07-25 — Developer — документация завершена, `make verify` чисто (см. Отчёт).

## Отчёт о выполнении

### Задача

TASK-084 — документация `apps/orchestrator`.

### Что сделано

- **`docs/architecture/orchestrator.md`** (новый) — по единой структуре документов раздела. Описывает **фактическую** реализацию, а не замысел:
  - роль в системе и то, какой именно участок golden path закрыт;
  - почему опрос журнала, а не подписка (внутрипроцессность шины по ADR-002), и почему курсор по `seq`, а не по времени;
  - как наполняется read-модель и почему без истории агент получил бы пустой промпт;
  - sequence diagram участка `TaskPlanned` → Review;
  - **обоснование порядка шагов** — включая то, что порядок «ветка до `StartTask`» выбран по итогам живого отказа, а не изначально;
  - принципы поведения при отказе;
  - **таблица ограничений с колонкой «что потребуется для снятия»** — чтобы каждое ограничение читалось как осознанное решение с известной ценой, а не как незаконченность.
- **`components.md`** — описание Orchestrator приведено к фактическому: было «назначение исполнителей ролей» (множественное число), реализована одна роль Developer; добавлена ссылка на новый документ, как это сделано для `memory.md`/`tools.md`.
- **`golden-path.md`** — существенная правка: документ утверждал «сценарий полностью аспирационный», что перестало быть правдой. Добавлен раздел «Что автоматизировано на сегодня» (участок Developer автоматизирован; PM/ревью/QA — по-прежнему человек через `apps/api`), а формулировка в «Назначении» переведена в исторический факт с отсылкой к этому разделу — иначе документ противоречил бы сам себе.
- **`apps/orchestrator/README.md`** — заглушка «полное описание появится в TASK-084» заменена реальной ссылкой.
- **`docs/roadmap/EPIC-010-orchestrator.md`** — отмечены выполненные критерии (2 из 7 закрыты этой задачей, TASK-085 закроет остальные), статусы задач в декомпозиции, и в «Риски» добавлены **два реализованных риска** — BUGFIX-004 и BUGFIX-005, найденные этим эпиком в уже закрытых эпиках, с общим для них уроком про тестирование швов между слоями.

### Изменённые файлы

- `docs/architecture/orchestrator.md` (новый).
- `docs/architecture/components.md` — описание Orchestrator, ссылка, дата.
- `docs/architecture/golden-path.md` — раздел о фактической автоматизации, правка «Назначения», дата.
- `apps/orchestrator/README.md` — ссылка вместо заглушки.
- `docs/roadmap/EPIC-010-orchestrator.md` — критерии, статусы, реализованные риски.

### Как проверялось

- `make verify` — чисто: markdownlint `0 issues` на 251 файле, docs-check — **1382 ссылки** проверено, ошибок 0, **12 mermaid-блоков** (новая диаграмма проходит валидацию).
- Сверка каждого утверждения нового документа с кодом: порядок шагов — по `dispatch.go`/`monitor.go`, ограничения — по фактическим константам и полям, обоснования — по тем же причинам, что записаны в комментариях кода. Документ не содержит утверждений, которых нет в реализации.
- Проверено, что `apps/orchestrator/README.md` больше не содержит заглушки-обещания (была бы устаревшей ссылкой на несуществующий документ).

### Обновлённая документация

Перечислена в «Изменённых файлах» — вся задача состоит из неё.

### Open Questions

Нет.

### Рекомендации

- **Пункт исходного scope оказался неприменим:** `docs/architecture/README.md` в проекте не существует — документы раздела связаны перекрёстными ссылками из `components.md`. Ссылка добавлена туда. Зафиксировано в плане, чтобы отсутствие индекса не выглядело пропущенным шагом.
- **Большая часть этой задачи была уже сделана** к моменту её начала: документация писалась в том же PR, что и код (правило CLAUDE.md), а не откладывалась «на потом». Это правильный порядок, но он делает отдельную задачу-документацию в конце эпика почти пустой. Для следующих эпиков стоит либо не выделять её отдельно, либо сразу формулировать как «архитектурный документ уровня раздела + синхронизация сквозных документов» — то, что действительно нельзя написать по частям.
- `golden-path.md` оказался устаревшим не из-за этого эпика, а потому что его статусная формулировка была привязана к дате введения. Стоит проверить остальные документы на такие же «замороженные» утверждения при стабилизации (последний эпик декомпозиции v1.0).
