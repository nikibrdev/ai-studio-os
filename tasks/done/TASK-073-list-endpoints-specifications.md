# TASK-073: Спецификации списковых операций (Documentation First)

## Тип

docs

## Эпик

[EPIC-009 Dashboard](../../docs/roadmap/EPIC-009-dashboard.md)

## Цель

Описать `GET /projects` и `GET /projects/{id}/tasks` в `docs/api/` до того, как Dashboard (TASK-075/076) начнёт их вызывать — тот же принцип Documentation First, что и TASK-066.

## Контекст

Операции реализованы в TASK-072; эта задача документирует их точную форму (запрос, ответ, коды ошибок) по шаблону [API.md](../../.claude/templates/API.md), дополняя уже существующие `docs/api/projects.md` и `docs/api/tasks.md`.

## Scope

### Входит

- `docs/api/projects.md` — операция `GET /projects`.
- `docs/api/tasks.md` — операция `GET /projects/{id}/tasks`.

### Не входит

- Пагинация/фильтрация — не описывается, поскольку не реализована (TASK-072).

## Критерии приёмки

- [x] Обе операции описаны в точности по фактически реализованному в TASK-072 (не наоборот).
- [x] `make verify` (markdownlint, docs-check) — чисто.

## Затрагиваемые модули и документы

- `docs/api/projects.md`, `docs/api/tasks.md`.

## Definition of Ready

- [x] Цель и результат сформулированы
- [x] Критерии приёмки определены
- [x] Затрагиваемые модули/документы указаны
- [x] Ограничения и зависимости указаны — зависит от TASK-072

## План реализации

1. `docs/api/projects.md` — новая операция «Список проектов» (`GET /projects`) перед «Создать проект», по формату уже существующих операций.
2. `docs/api/tasks.md` — новая операция «Список задач проекта» (`GET /projects/{projectId}/tasks`) после «Получить состояние задачи»; заодно обновить уже существующую пометку «Ограничение» у `GET /projects/{projectId}/tasks/{id}» — она явно указывала на отсутствие списковых операций, теперь снята с пометкой «снято в TASK-072».
3. `make verify`.

## История

2026-07-22 — Architect — EPIC-009 открыт; задача поставлена в очередь.

2026-07-23 — Developer — задача взята в работу и завершена (см. Отчёт).

## Отчёт о выполнении

### Задача

TASK-073 — спецификации `GET /projects` и `GET /projects/{id}/tasks`.

### Что сделано

- `docs/api/projects.md` — добавлена операция «Список проектов»: запрос/ответ/ошибки/события по формату уже существующих операций, явно указано `200` с `[]` для пустого результата.
- `docs/api/tasks.md` — добавлена операция «Список задач проекта»: запрос/ответ/ошибки/события, изоляция по `projectId` (та же гарантия, что BUGFIX-003 дал хранению); уже существующая пометка «Ограничение» у операции чтения одной задачи обновлена — явно отмечено, что она снята этой же задачей.
- Название задачи («Documentation First») формально описывает эту документацию как предшествующую потребителям API (Dashboard, TASK-075/076) — не реализации операций (TASK-072 уже реализовал их раньше по декомпозиции эпика); зависимость TASK-073 → TASK-072 зафиксирована в самом файле задачи с самого начала, никакого расхождения с планом нет.

### Изменённые файлы

- `docs/api/projects.md`, `docs/api/tasks.md`.

### Как проверялось

- `make verify` (markdownlint, docs-check) — чисто.

### Обновлённая документация

Вся документация, перечисленная в «Изменённые файлы» — задача целиком документационная.

### Open Questions

Нет.

### Рекомендации

Нет.
