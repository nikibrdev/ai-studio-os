# TASK-017: Детальный конфиг markdownlint; попытка mermaid-cli

## Тип

chore

## Эпик

EPIC-002.5 ([docs/roadmap/EPIC-002.5-engineering-platform.md](../../docs/roadmap/EPIC-002.5-engineering-platform.md))

## Цель

Закрыть технический долг базового конфига (TASK-013): нормализовать стиль разделителей таблиц и вернуть правило MD060; оценить целесообразность mermaid-cli для полной валидации диаграмм («при возможности» по плану эпика).

## Контекст

TASK-013 отключил MD060 (252 нарушения — все одного типа: компактный стиль `|---|`), явно пометив вопрос как отложенный сюда. Текущая проверка диаграмм (`scripts/verify-docs.sh`) — структурная (парность ограждений `mermaid` + известный тип диаграммы первой строкой), не настоящий рендер.

## Scope

### Входит

- `npx markdownlint-cli2 --fix` — нормализация разделителей таблиц (`|---|` → `| --- |`) в 34 файлах; MD060 включён обратно (через `"default": true`, явный override убран).
- Каталог снимков метрик исключён из `make md-lint` (`Makefile`, глоб `#engineering/metrics/**`) — снимки неизменяемы по правилу их README, автофикс не должен их трогать.
- Оценка mermaid-cli — см. Отчёт.

### Не входит

- Подключение mermaid-cli в CI (решение — не делать сейчас, обоснование ниже).

## Критерии приёмки

- [ ] `make md-lint` (MD060 включён) — 0 issues.
- [ ] Снимки `engineering/metrics/` не изменены автофиксом.
- [ ] Диаграммы (fenced-блоки ```mermaid) не задеты нормализацией таблиц.
- [ ] Прогон verify в PR — зелёный.

## План реализации

1. `npx markdownlint-cli2 --fix` для нормализации таблиц; проверить `git diff --stat` — изменений должно быть только по разделителям таблиц (символ `|`), без затрагивания mermaid-блоков.
2. Включить MD060 (убрать явный `false`); прогнать `make verify` локально.
3. Восстановить `engineering/metrics/2026-07-20.md` к закоммиченному состоянию (нарушение принципа неизменности снимка недопустимо); исключить каталог из `md-lint`.
4. Попробовать `npx @mermaid-js/mermaid-cli` локально на реальном файле с диаграммой; по результату — включить в CI либо задокументировать отказ.

## Затрагиваемые модули и документы

- `.markdownlint.jsonc`, `Makefile`, 34 md-файла (только разделители таблиц), `engineering/metrics/README.md`.

## Definition of Ready

- [x] Цель и результат сформулированы
- [x] Критерии приёмки определены
- [x] Затрагиваемые модули/документы указаны
- [x] Ограничения и зависимости указаны

## История

2026-07-20 — Architect — сигнал на продолжение EPIC-002.5.
2026-07-20 — Claude Code (Developer) — ready → in-progress → выполнена, переведена в review (PR #6).

## Отчёт о выполнении

1. **Задача:** TASK-017 — детальный конфиг markdownlint + попытка mermaid-cli.
2. **Что сделано:**
   - `npx markdownlint-cli2 --fix` нормализовал разделители таблиц в 34 файлах (252 находки MD060 → 0); проверено `git diff` — задеты только строки `|---|` → `| --- |`, ни один блок ```mermaid не изменён (сверено построчно на state-machine.md).
   - MD060 включён обратно; `.markdownlint.jsonc` очищен от временных комментариев про TASK-017.
   - `engineering/metrics/2026-07-20.md` — единственный файл, который автофикс тоже задел (это правкоспособный markdown, ничем формально не защищённый). Правило README требует неизменности снимков, поэтому я вручную вернул файл к закоммиченному виду (`git checkout HEAD --`) и исключил `engineering/metrics/**` из `make md-lint`, чтобы линтер больше не трогал снимки.
   - **mermaid-cli:** попробовал `npx @mermaid-js/mermaid-cli` на `docs/architecture/state-machine.md` (реальная диаграмма). Результат: команда завершилась с кодом 0, но не создала ни выходного `.md`, ни `.svg` — молчаливый отказ без диагностики (вероятно, puppeteer/chromium не поднимается в этом Windows/Git-Bash окружении). **Решение: не подключать в CI сейчас.** Молчаливый отказ хуже явной ошибки — CI мог бы «зелёным» пропускать сломанную диаграмму. Пункт плана эпика был сформулирован как необязательный («при возможности») — оставляю текущую структурную проверку (`scripts/verify-docs.sh`: парность ограждений + известный тип диаграммы) как основную линию защиты; настоящий рендеринг — по отдельной задаче, когда будет возможность протестировать в среде, ближе к CI (Linux), а не блокировать эпик хрупкой зависимостью.
3. **Изменённые файлы:** `.markdownlint.jsonc`, `Makefile`, `engineering/metrics/README.md`, 34 md-файла (только разделители таблиц), файл задачи.
4. **Как проверялось:** локально — `make verify`-эквивалент (fmt/lint/vet/build — не изменялись, но прогнаны для целостности) + `markdownlint-cli2` (0 issues) + `verify-docs.sh` (585 ссылок, 9 mermaid-блоков, 0 ошибок); в CI — прогон этого PR.
5. **Обновлённая документация:** `engineering/metrics/README.md` (исключение из md-lint).
6. **Open Questions:** нет технических; зафиксировано инженерное решение — не подключать mermaid-cli при текущей невозможности локально проверить его надёжность (соответствует правилу необратимости: не кодировать то, в чём нет уверенности).
7. **Рекомендации:** если mermaid-cli понадобится (например, для рендера диаграмм в документацию как изображения), тестировать сначала прямо в GitHub Actions (Linux-раннер) через отдельный PR, не блокирующий текущий verify.
