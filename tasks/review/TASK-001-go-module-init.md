# TASK-001: Инициализация Go-модуля и инструментов качества

## Тип

chore

## Эпик

EPIC-002 ([docs/roadmap/EPIC-002-foundation.md](../../docs/roadmap/EPIC-002-foundation.md))

## Цель

Проект собирается стандартными командами: единый `go.mod` (Go 1.24, модуль `ai-studio-os`), конфигурация golangci-lint, рабочие цели Makefile (build/fmt/lint).

## Контекст

[ADR-009](../../docs/adr/ADR-009-toolchain.md) (принят): Go 1.24, golangci-lint, gofumpt, единый `go.mod`. Кода ещё нет — модуль пустой.

## Scope

### Входит

- `go.mod` в корне (module `ai-studio-os`, go 1.24).
- `.golangci.yml` — минимальная конфигурация с gofumpt/goimports.
- Обновление Makefile: реальные цели build, fmt, lint; test — заглушка до v0.4.

### Не входит

- Любой Go-код; CI/CD; установка инструментов в систему пользователя.

## Критерии приёмки

- [ ] `go build ./...` выполняется без ошибок.
- [ ] `go vet ./...` выполняется без ошибок.
- [ ] Makefile отражает команды ADR-009.

## Затрагиваемые модули и документы

- `go.mod`, `.golangci.yml`, `Makefile` (корень).

## Definition of Ready

- [x] Цель и результат сформулированы
- [x] Критерии приёмки определены
- [x] Затрагиваемые модули/документы указаны
- [x] Ограничения и зависимости указаны

## История

2026-07-19 — Architect — задача определена решением по EPIC-002 (Foundation, малые итерации).
2026-07-19 — Claude Code (Developer) — задача оформлена в ready.
2026-07-19 — Claude Code (Developer) — взята в работу (in-progress), выполнена, переведена в review.

## Отчёт о выполнении

1. **Задача:** TASK-001 — инициализация Go-модуля и инструментов качества.
2. **Что сделано:** создан `go.mod` (module `ai-studio-os`, go 1.24); создан `.golangci.yml` (формат v2: formatters gofumpt/goimports, линтеры по умолчанию + misspell, revive); Makefile переведён с заглушек на реальные цели build/vet/fmt/lint (test — заглушка до v0.4).
3. **Изменённые файлы:** `go.mod` (новый), `.golangci.yml` (новый), `Makefile` (обновлён).
4. **Как проверялось:** `go build ./...` — успех (Go 1.24.5; предупреждение «matched no packages» ожидаемо — пакетов ещё нет); `go vet ./...` — «no packages to vet» (ожидаемо). Конфигурация golangci-lint не прогонялась (линтер не установлен в окружении) — проверить при первом прогоне.
5. **Обновлённая документация:** не требуется (Makefile-цели соответствуют ADR-009; документы обновлены на этапе Freeze).
6. **Open Questions:** имя модуля `ai-studio-os` — локальное; при публикации на GitHub заменить на полный путь (зафиксировано в ADR-009). golangci-lint отсутствует в окружении разработки — установить перед первым `make lint`.
7. **Рекомендации:** после `git init` добавить проверку build/vet как минимальный обязательный шаг процесса.
